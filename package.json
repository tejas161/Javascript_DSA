{
  "name": "javascript_dsa",
  "version": "1.0.0",
  "description": "1. Create a Specific Folder for the particular Data Structure (only if not created already), add your Code File with proper `Name` for the Data Structure / Algorithm. 2. Maintain `Code Readibility`, add `appropriate comments` in the Code File wherever needed.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint": "eslint ."
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@eslint/js": "^9.7.0",
    "eslint": "^9.7.0",
    "globals": "^15.8.0"
  }
}
